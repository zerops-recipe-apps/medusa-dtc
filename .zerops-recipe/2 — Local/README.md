# Medusa DTC — Local Environment

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Local** environment runs the production DTC backend and storefront in Zerops together with the managed data plane. Use zCLI VPN to point a laptop `yarn develop` at `db` / `redis` / `storage`, or keep the staged apps as the live preview.
<!-- #ZEROPS_EXTRACT_END:intro# -->
