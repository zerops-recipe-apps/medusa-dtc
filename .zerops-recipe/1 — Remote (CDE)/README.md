# Medusa DTC — Remote Environment

<!-- #ZEROPS_EXTRACT_START:intro# -->
**Remote (CDE)** environment mirrors the agent topology — production `medusa` and `nextstore` plus hobby data services — so a developer can SSH into either Node container and iterate without installing Postgres, Valkey, or MinIO locally.
<!-- #ZEROPS_EXTRACT_END:intro# -->
